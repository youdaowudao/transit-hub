# 分组贡献利润瞬时超时修复验证记录

## 实际结果

- 只读运行态探测确认：当前已连接上游曾在 12 秒请求窗口内超时，但后续请求恢复成功；这与“单次抖动使整张利润图 fail-closed”一致。
- 新增 key/token 统计重试回归：sub2api 和 new-api 的首次临时失败后恢复、401 不重试、连续失败在固定次数后停止，全部通过。
- 受影响模块定向测试及全量 Go 测试、race、vet、格式检查均通过；前端类型检查、Vitest 和生产构建也通过。
- 固定服务已重启并核验：前端 `5444`、后端 `5555` 健康，进程归属当前仓库；PostgreSQL 为原 `transithub` / `postgres`，关键数据计数与重启前一致，Redis 为 `PONG`。

## 行为边界与剩余风险

- 每个 key/token 用量请求最多尝试 2 次，每次最长 20 秒，重试间隔 150 毫秒；现有每站点最多 4 个并发查询保持不变。
- 连续上游故障、鉴权失败或普通 4xx 仍不会生成利润数据，页面继续提示不可用，避免未知成本被当成零。
