# 实施计划

1. 在 Sub2API 只读客户端增加兑换码分页读取与严格解析，补充请求取消、超时和响应体大小限制。
2. 在现有批量邮件服务边界内完成全量分页、稳定性校验和按用户聚合，并暴露管理员只读接口。
3. 在“用户最后使用”页面增加同页查询模式，展示邮箱及聚合数据，复用现有邮箱复制状态。
4. 增加定向后端、前端行为测试，完成构建和浏览器验证。
5. 更新版本与验证记录，重启并核对固定开发服务。
