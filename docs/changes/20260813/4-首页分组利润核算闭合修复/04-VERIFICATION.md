# 验证记录

状态：已验证，待用户审阅提交。

## 必须通过的定向测试

| 场景 | 必须证明的结果 |
| --- | --- |
| 同组两条及以上连接 | 分组成本为全部 Key 成本之和；分组利润只计算一次；交换输入顺序结果相同。 |
| 一条 exact、一条失败 | 分组保留权威营收，`todayCost`、`todayProfit` 均为 `null`；全局不返回总成本、总利润。 |
| 一条 exact、一条 unallocatable | 计数和问题按连接闭合；分组不显示正式利润。 |
| 多分组连接 | 连接被标为 `unallocatable`，带 `connectionId` 问题；不允许静默遗漏。 |
| 重复 Key / 重复账号 | 每条受影响连接都为 `unallocatable`，不分摊成本或营收。 |
| 主站成员关系变化 | 唯一当前成员且 Key 存在时自动转移本地绑定；账号停用不影响已有 Key 的归属；无分组、多分组、重复绑定或 Key 缺失自动本地解绑；分组权威营收仍显示。 |
| 连接级营收查询失败 | 受影响连接逐条 `failed`，分组营收仍显示，分组不显示成本或利润。 |
| 分组营收与连接营收不一致 | 记录可定位的对账问题，分组不显示成本或利润。 |
| 真实零成本 | Key 成本零值仍为已读取成本，可在其他条件满足时得到精确利润。 |
| 真实负利润 | 全部闭合后仍按实际负数返回并在利润图展示。 |
| 未绑定上游成本 | 作为“分组外上游成本”贡献显示实际成本和负利润，计入分组贡献汇总；不分摊给主站分组，不进入连接计数或问题数。 |
| 全局 partial | 精确分组可展示利润；非精确分组不展示；总利润和前三分组利润占比不可用。 |
| 未绑定成本存在 | 精确分组和分组贡献汇总仍可显示；未绑定成本单列为贡献，不分摊到任何主站分组。 |

## 协议与界面检查

- Sub2API 分组汇总解析测试覆盖稳定 `group_id`、指定业务日和金额字段；不以分组名称归集。
- Go 定向测试覆盖归因顺序稳定、质量闭合、成员变化、查询失败、对账不一致和零成本。
- 前端定向测试或可重复页面检查覆盖利润模式的 `exact` 二次筛选、营收模式完整展示和总利润不可用状态。
- 运行前端类型检查、前端构建和 `git diff --check`。

## 本地运行验收

- 重启前记录 `5444`、`5555` 的实际命令、工作目录、环境来源及脱敏连接目标；只停止已确认的当前项目旧实例。
- 重启后确认两个固定端口均由当前项目唯一实例监听，前后端健康检查成功。
- 确认 PostgreSQL 集群、数据库、用户、当前工作区和关键数据数量与重启前一致；不得创建、初始化、切换或修改数据库。
- 用本地同组多真实接入数据核对：权威分组营收、每条连接营收、每条 Key 成本、分组累计成本和分组利润的算式一致。

## 实际结果

### 代码与协议

- `go test ./internal/modules/dashboard ./internal/modules/my_sites ./internal/modules/upstream -count=1`：通过。
- 覆盖同组多连接后置聚合、输入顺序稳定、部分失败、成员变化自动转移/解绑、账号停用但 Key 仍有效时的转移、营收对账不一致、多分组、重复 Key/账号、零成本、负利润、分组外成本贡献和连接计数闭合。
- 未绑定成本返回 `unboundUpstreamCost` 摘要及明确的“分组外上游成本”贡献，不再进入 `issues` 或“核算问题”数量；阻断问题数量按真实连接终态统计。
- Sub2API 协议测试覆盖稳定 `group_id`、日期与 `Asia/Shanghai`、管理员鉴权、缺字段、重复 ID，以及明确空 `data.groups` 的合法零流水响应。
- `npm test -- tests/dashboard-group-profit.test.ts`：5 项通过。
- `npm run typecheck`：通过。
- `npm run build`：通过；仅有既存的 Rollup `@vueuse` PURE 注释警告。
- `git diff --check`：通过。

### 本地真实接口核对

- 读取当前本地 Redis 中已有的 TransitHub Sub2API 管理会话，仅在当前 shell 内使用凭据，未输出令牌、未创建或修改会话。
- `2026-08-13` 的真实响应为 `data.groups: []`，响应结构有效，确认空数组代表当天零分组流水，不应被判为接口错误。
- `2026-05-25` 的真实响应包含 1 条分组记录，金额合计 `10.6592546`，记录含稳定 `group_id`；说明非空日期仍按 ID 和金额解析。
- 服务器截图中的历史连接和金额不在本地，未将本地金额冒充截图复算结果。

### 双端运行验收

- 重启前后均确认实际启动器、工作目录和进程组：前端使用 `npm run dev -- --host 127.0.0.1 --port 5444`，后端使用 `go run ./cmd/api`；当前仓库真实路径为 `/home/ss/data/projects/transit-hub` 的同一 checkout。
- 重启后 `5444`、`5555` 各只有当前项目实例监听；直连和前端代理 `/api/health` 均返回 `200`。
- 最新运行日志显示自动纠正后 `expected=12 resolved=12 failed=0 unallocatable=0 issues=0`；未绑定成本作为“分组外上游成本”独立贡献，不再进入“核算问题”，计入分组贡献汇总但不伪造为任何主站分组。首页全局成本、净利润仍由独立首页指标口径计算。
- 后端环境仍为 `127.0.0.1:5432/transithub` 和 `127.0.0.1:6379/0`；PostgreSQL 用户/数据库未变化，Redis `PING` 返回 `PONG`。
- 重启前后关键数量由自动纠正更新为：用户 `1`、workspace `2`、上游站点 `4`、真实接入 `12`、连接健康状态 `15`；当前 workspace 未变化。减少的一条是已不存在上游 Key 的本地绑定，不涉及删除上游资源。
- 浏览器已打开本地登录页并在验证结束后关闭；由于当前浏览器没有登录态且未获得账号凭据，未宣称已完成登录后的图表验收。

### 版本与边界

- 代码内置版本已统一为 `V2.2.0`；本轮未提交、未推送、未部署。
- 本次运行验收已触发自动核对写入：只更正 TransitHub 本地连接和价格映射，不会修改或删除主站分组、账号或 Key；不新增 `membershipUnknown`，不把 NewAPI 引入主站核算，也不做负数钳制。

### 当前本地数据复核

- 自动纠正前 `real_connections` 有 13 条 active 记录；账号 `73` 的本地绑定为组 `3`，账号 `92` 的本地绑定为组 `4`。当前本地绑定为 12 条。
- 使用首页同一 Sub2API 管理会话读取主站成员：分组 `3`、`4` 当前账号数均为 `0`；账号 `92` 当前在组 `5`。其本地绑定已由 `4` 原子转移到 `5`。账号 `73` 的 Key `5997` 已不在上游 Key 成本快照中，其本地绑定已解除。
- 另发现账号 `103` 的本地账号状态为 inactive，但 Key `2733` 仍存在且有成本、当前唯一分组为 `5`；该记录已恢复并转移到组 `5`，不会因账号状态被错误丢弃。
- Key/账号重复检查为 `0`；当前 12 条真实接入全部闭合，`expected=12 resolved=12 failed=0 unallocatable=0 issues=0`。最后一次实算中，组 `5` 的成本/利润均为 `4.379087/-4.379087`，组 `7` 为 `0/0`；另有无真实接入关系的上游 Key 成本 `15.751661`，已作为分组外成本贡献显示。分组贡献总成本/总利润为 `20.130747/-20.130747`，不再伪造成问题或任意分组。
