# 健康档位与上游倍率解析修复任务

## A. Priority

- [x] 增加健康档末位计算，并让倍率不可用的健康探活目标继续受托管。
- [x] 保留 `multiplier_only`、Priority 空值、人工冲突和不完整清单保护。
- [x] 增加倍率缺失、查询暂不可用及健康档切换测试。

## B. 上游分页

- [x] 为 Sub2API Key 列表补齐完整分页。
- [x] 为 NewAPI Token 列表补齐完整分页。
- [x] 增加第二页命中、分页结束和错误中止测试。

## C. 页面解释

- [x] 后端只返回本轮真实可用的有效倍率，不用历史同步倍率冒充当前来源。
- [x] 前端分别显示 `unassociated`、`missing`、`conflict`、`unavailable` 的具体说明。
- [x] 增加前端文案和分支回归测试。

## D. 收口

- [x] 同步 `V2.2.5` 版本。
- [x] 完成定向测试、构建和残留检查。
- [x] 重启并核对固定前后端及原数据库、Redis。
- [x] 更新验证文档和排队状态。
- [x] 核对提交边界后提交并推送 `origin/dev`，创建或更新 `dev -> main` PR，开启普通 Merge commit 自动合并。
