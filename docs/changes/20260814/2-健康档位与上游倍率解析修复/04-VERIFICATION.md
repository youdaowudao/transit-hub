# 健康档位与上游倍率解析修复验证

版本目标：`V2.2.5`

## 验证项目

- Priority：倍率缺失和查询暂不可用时，健康状态仍进入正确档末位；倍率可用时原排序不变。
- 分页：Sub2API 和 NewAPI 的第二页 Key 可以被读取；中间页错误不返回部分列表。
- 页面：四种倍率解析状态显示具体原因，无倍率时不显示历史倍率。
- 回归：`multiplier_only`、人工冲突、Priority 空值、策略退出恢复原值保持原行为。
- 运行：固定 `5444`、`5555` 使用当前仓库和原数据库、Redis 启动，健康检查通过。

## 实际结果

- Priority 定向回归：`go test ./internal/modules/connection_health` 通过；覆盖倍率缺失、查询暂不可用、健康档位末位、`multiplier_only`、人工冲突、空 Priority、恢复和充值换算行为。
- 上游定向回归：`go test ./internal/modules/upstream` 通过；新增 Sub2API/NewAPI 第二页命中和中间页错误中止测试，现有上下文取消测试通过。
- 前端行为回归：6 个连接健康测试文件、22 项测试通过；包含四种倍率解析状态文案。
- 前端 `npm run typecheck` 通过；`npm run build` 通过。构建仅报告 `@vueuse/core` 既有 Rollup `/*#__PURE__*/` 注释位置警告，不影响产物。
- 本地运行服务已按原工作目录和连接重启：前端 `5444`、后端 `5555` 各一个当前仓库实例，前后端健康检查均返回 `200`，Redis 原连接返回 `PONG`。
- PostgreSQL 只读核对为 `transithub` / `postgres` / PostgreSQL 16.14；用户 `1`、工作区 `2`、上游站点 `4`（启用 `4`）、真实连接 `12`、健康状态 `15`，与重启前运行快照一致。
- 新进程调度快照显示健康目标位于 `10-99`，降级/观察目标位于 `1000-9999`，暂停目标位于 `100000`；无有效倍率目标的同步快照保持 `effective_multiplier=0`，没有猜测 `1x`。
- 工作区代码、测试和文档均通过 `git diff --check`；测试结束后无临时测试进程、额外端口或临时检查文件残留。

## 剩余风险

- 本地现有上游站点 Key 数量未超过 100，分页完整性由合成 150 条数据的两平台测试证明；真实大列表仍依赖上游接口正确返回分页元数据或正常末页。
- 本轮未做需要真实登录态的浏览器验收；后端运行态、数据库同步快照、前端构建和行为测试已覆盖本次逻辑边界。

## 收口判定

代码、测试、版本和验证均完成；固定开发服务保持运行，提交、推送和 PR 收口在本轮完成。
