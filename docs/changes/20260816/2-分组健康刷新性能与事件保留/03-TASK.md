# 执行任务清单

## 变更前

- [ ] 读取并确认服务器实际服务、工作目录、数据库集群、数据库名、用户和当前工作区（生产尚未授权核验）。
- [ ] 取得生产只读基线：接口分阶段耗时、上游请求数量/分页、事件行数、表和索引大小、三个历史查询计划（生产尚未执行）。
- [x] 本地建立可删除行回滚表，记录待删除事件为 `18,995` 行；覆盖条件为早于 23 小时，正式清理条件仍为 24 小时。

## 实施

- [x] 完成查询/索引方案的定向测试，确认时间边界、排序、锁跳过、失败传播和取消行为。
- [x] 本地按批量方式执行 24 小时以前事件清理，避免长事务；清理后保留最近 24 小时事件。
- [x] 清理代码不删除 `connection_health_states`、策略、动作、预算、站点、账号和工作区数据。
- [x] 未修改网络超时、500、工作区错误和现有错误展示。
- [x] 本地验证了批量上限、租约、索引门禁、锁等待跳过、失败停止和幂等行为；生产批次耗时尚未取得。

## 实施后

- [x] 本地对比刷新端到端耗时及本地 SQL、上游 HTTP/元数据、组装阶段耗时。
- [x] 本地核对健康状态、策略、预算、账号和 workspace 指针；动作状态行在调度器运行期间自然变化，未作为清理不变量误判。
- [x] 本地核对 24 小时窗口内事件仍在，旧审计和旧动作归因缺失已按本变更接受。
- [x] 未执行 `VACUUM FULL`；本轮不需要额外回收文件空间。
- [x] 本地回滚表在删除验证完成后已删除；生产仍未执行清理，正式上线前必须重新建立生产备份和恢复路径。

## 当前阶段

- [x] 已完成本地代码、定向测试、真实 PostgreSQL 合约测试和本地数据库受控清理验证。
- [x] 已重启并核对固定开发服务 `5444/5555`；生产数据库仍保持未操作状态。
- [ ] 服务器只读核验、服务器分阶段耗时和正式生产清理待单独授权。
